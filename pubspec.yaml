name: file_transfer
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  args: ^2.3.1
  shelf: ^1.3.2
  shelf_router: ^1.1.3

dev_dependencies:
  http: ^0.13.5
  lints: ^2.0.0
  test: ^1.21.6
